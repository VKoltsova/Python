Python. HW_1
Выгрузить файл в Git репозиторий.

# 1) Создать переменную типа String
s = "string"

# 2) Создать переменную типа Integer
i = 15

# 3) Создать переменную типа Float (число с плавающей точкой)
fl = 12.6

# 4) Создать переменную типа Bytes
b = bytes([0, 26, 38])

# 5) Создать переменную типа List
l = ['a', 'b', 'c', 'd']

# 6) Создать переменную типа Tuple - кортеж (списки, но это неизмен стр-ра данных)
t = (13, 4, 'c', 12.6)

# 7) Создать переменную типа Set - множество (контейнер, содерж не повтор эл-ты в случ порядке)
se = {'h', 'e', 'l', 'l', 'o'}

# 8. Создать переменную типа Frozen set - как set, только неизммен тип данных
froz = frozenset ('hello')

# 9) Создать переменную типа Dict - словарь -  ключ:начение
d = {"one": 1, "two": 2, "three":3, "four": 4}

# 10) Вывести в консоль все выше перечисленные переменные с добавлением типа данных.
print("s = ", type(s))
print("i = ", type(i))
print("fl = ", type(fl))
print("b = ", type(b))
print("l = ", type(l))
print("t = ", type(t))
print("se = ",type(se))
print("froz = ",type(froz))
print("d = ",type(d))
# 11) Создать 2 переменные String, создать переменную в которой суммируете эти переменные. Вывести в консоль.

a = "Vika"
b = "Taratuta"
c = a + b
print("a + b = ", (c))

# 12) Создать 2 переменные Integer, создать переменную в которой суммируете эти переменные. Вывести в консоль.

a1 = 9
a2 = 4
a3 = a1 + a2
print("a3 = ", a3)

# 13) Создать переменную в которой Разделите эти переменные Int. Вывести в консоль.
a4 = a1/a2
print("a4 = ", a4)

# 14) Создать переменную в которой Умножите эти переменные Int. Вывести в консоль.
a5 = a1 * a2
print("a5 = ", a5)

# 15) Создать переменную в которой Разделите без остатка эти переменные Int. Вывести в консоль.

a6 = a1 // a2
print("a6 = ", a6)

# 16) Создать переменную в которой надо присвоить остаток от деления этих переменные Int. Вывести в консоль.

a7 = a1 % a2
print("a7 = ", a7)


# Обновленное дз
# 1) Создать переменную типа String
s = 'hello'

# 2) Создать переменную типа Integer

i = int (5)

# 3) Создать переменную типа Float

f = float (6.9)

# 4) Создать переменную типа Bytes

b = bytes ([0, 55, 255])

# 5) Создать переменную типа List

l = ['h', 'e', 'l', 'l', 'o']

# 6) Создать переменную типа Tuple

t = tuple ('hello, world')

# 7) Создать переменную типа Set

se = {'t', 'o', 'd', 'a', 'y'}

# 8. Создать переменную типа Frozen set

fr = frozenset ('new')

# 9) Создать переменную типа Dict

d = {"Ann": "tester", "Vera": "programmer"}


# 10) Вывести в консоль все выше перечисленные переменные с добавлением типа данных.
print(s, type(s))
print(i, type(i))
print(f, type(f))
print(b, type(b))
print(l, type(l))
print(t, type(t))
print(se, type(se))
print(fr, type(fr))
print(d, type(d))

# 11) Создать 2 переменные String, создать переменную в которой сканкатенируете эти переменные. Вывести в консоль.
s1 = 'Pretty'
s2 = 'Girl'
s3 = s1 + s2
print("s1 + s2 = ", s3)

# 12) Вывести в одну строку переменные типа String и Integer используя “,” (Запятую)

print(s, i)
# 13) Вывести в одну строку переменные типа String и Integer используя “+” (Плюс)

print(s + ' ' + str(i))

s = "string "
i = 10
print(s + str(i))
